language: php

sudo: false

cache:
    directories:
      - $HOME/.composer/cache

before_script:
    - composer install

script: phpunit

matrix:
    include:
        - php: 5.5
        - php: 5.5
        - php: 5.6
        - php: 7.0
        - php: hhvm